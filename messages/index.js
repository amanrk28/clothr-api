const commonMsg = {
    serverError: 'Server Error!',
    dbError: 'Unable to save data in DB',
    accessDenied: 'ACCESS DENIED',
    notFound: field => `${field} not found`,
};

module.exports = { commonMsg };
